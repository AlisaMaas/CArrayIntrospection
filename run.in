#!/bin/sh -e

top="$(dirname "$0")"
export SHELL=/bin/sh

. /etc/profile.d/modules.sh
export -f module
exec bash '@SAGE@' -sh -c "env PYTHONPATH=\$PYTHONPATH:'$top/SRA/SAGE/Runtime' '@LLVM_BINDIR@/opt' -load '$top/CArrayIntrospection.so' $*"
